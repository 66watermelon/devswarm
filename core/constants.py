"""
DevSwarm 全局常量定义

集中管理 Pub/Sub 协议标记、LangGraph 节点过滤规则、SSE 展示名映射等。
避免散落在路由文件中的魔法字符串。
"""

# ================================================================
# Redis Pub/Sub 协议常量
# ================================================================
EOF_MARKER = "[EOF]"

# ================================================================
# LangGraph 节点过滤规则
# ================================================================

# 不对外推送 SSE 事件的内部路由节点
SKIP_NODES: set[str] = {"intent_recognizer"}

# 节点名 → SSE 展示名映射（chat_agent 复用 tutor 身份，与前端 getNodeName 对齐）
NODE_DISPLAY_MAP: dict[str, str] = {"chat_agent": "tutor"}

# ================================================================
# GraphRAG 记忆提取规则
# ================================================================

# 需要被 LLM 提取的"前台角色"名单（对话中可见的 Agent）
EXTRACTABLE_ROLES: set[str] = {"tutor", "chat_agent"}
